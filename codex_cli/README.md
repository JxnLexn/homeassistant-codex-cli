# Codex CLI

Codex CLI als SSH-erreichbarer Arbeitsbereich für Home Assistant OS.

Die App bindet die Home-Assistant-Konfiguration unter `/homeassistant` ein und
stellt `/config` nur als Kompatibilitätslink bereit.
